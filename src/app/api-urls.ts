import {environment} from '../environments/environment';

export const USER_API_URL = `${environment.userApiUrl}`;
export const CITY_API_URL = `${environment.cityApiUrl}`;
export const NATIONALITY_API_URL = `${environment.nationalityApiUrl}`;
export const AUTH_API_URL = `${environment.authApiUrl}`;
