import {environment} from '../environments/environment';

export const AUTH_API_URL = `${environment.baseApiUrl}/auth`;

export const USER_API_URL = `${environment.baseApiUrl}/users`;
export const SERVICE_API_URL = `${environment.baseApiUrl}/services`;
export const CITY_API_URL = `${environment.baseApiUrl}/cities`;
export const NATIONALITY_API_URL = `${environment.baseApiUrl}/nationalities`;
export const RESERVATION_API_URL = `${environment.baseApiUrl}/reservations`;
export const USER_SERVICE_API_URL = `${environment.baseApiUrl}/user-services`;
export const API_URL = environment.apiUrl;
export const IMAGE_API_URL = `${environment.baseApiUrl}/images`;

